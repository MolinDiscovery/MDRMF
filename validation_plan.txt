ChatGPT conversation: Config Validation and Structure

Idea of YAML validation for MDRMF

1. Schema validation
    a. create schema file

2. Semantic checks
    a. create a list of checks that needs to happen.

3. Pre-run with a small subset of the full dataset
    a. figure out how to run with a subset of the dataset
    b. figure out the proper exceptions

4. Do a schema validation, semantic checks and pre-run before running the actual experiment.